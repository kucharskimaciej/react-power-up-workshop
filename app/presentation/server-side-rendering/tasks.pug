extends ~layouts/columns

block title
    | Tasks

block content
    +slide_column()
        .l-column-fit
            +task('basic')
                | Replace `React.Component` with `PureComponents` where appropriate

            +task('basic')
                | Remove the performance antipatterns from the application's code

            +task('optional')
                | Remove wasted rerenders (`perf-addon`/`why-did-you-update`)

